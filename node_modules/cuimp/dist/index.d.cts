interface CuimpDescriptor {
    browser?: string;
    version?: string;
    architecture?: string;
    platform?: string;
    forceDownload?: boolean;
}
/**
 * Cookie jar configuration options
 * - true: Use automatic in-memory cookie management with temp file
 * - string: Path to a custom cookie file (Netscape format)
 * - false/undefined: No automatic cookie management (default)
 */
type CookieJarOption = boolean | string;
interface BinaryInfo {
    binaryPath: string;
    isDownloaded: boolean;
    version?: string;
}
type Method = 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE' | 'HEAD' | 'OPTIONS';
interface CuimpRequestConfig {
    url?: string;
    method?: Method;
    baseURL?: string;
    headers?: Record<string, string | number | boolean>;
    params?: Record<string, string | number | boolean | undefined>;
    data?: string | Buffer | JSONValue | URLSearchParams;
    timeout?: number;
    maxRedirects?: number;
    proxy?: string;
    insecureTLS?: boolean;
    signal?: AbortSignal;
    extraCurlArgs?: string[];
}
interface CuimpResponse<T = JSONValue> {
    status: number;
    statusText: string;
    headers: Record<string, string>;
    data: T;
    rawBody: Buffer;
    request: {
        url: string;
        method: Method;
        headers: Record<string, string | number | boolean>;
        command: string;
    };
}
interface CuimpStreamHeaders {
    status: number;
    statusText: string;
    headers: Record<string, string>;
}
interface CuimpStreamResponse extends CuimpStreamHeaders {
    rawBody?: Buffer;
    request: {
        url: string;
        method: Method;
        headers: Record<string, string | number | boolean>;
        command: string;
    };
}
interface CuimpStreamHandlers {
    onHeaders?: (info: CuimpStreamHeaders) => void | Promise<void>;
    onData?: (chunk: Buffer) => void | Promise<void>;
    onEnd?: (info: CuimpStreamResponse) => void | Promise<void>;
    onError?: (error: Error) => void | Promise<void>;
    collectBody?: boolean;
}
interface Logger {
    info(...args: (string | number | boolean | object | null | undefined)[]): void;
    warn(...args: (string | number | boolean | object | null | undefined)[]): void;
    error(...args: (string | number | boolean | object | null | undefined)[]): void;
    debug(...args: (string | number | boolean | object | null | undefined)[]): void;
}
interface CuimpOptions {
    descriptor?: CuimpDescriptor;
    path?: string;
    extraCurlArgs?: string[];
    logger?: Logger;
    proxy?: string;
    cookieJar?: CookieJarOption;
    autoDownload?: boolean;
}
type JSONValue = string | number | boolean | null | JSONValue[] | {
    [key: string]: JSONValue;
};
interface CuimpInstance {
    request<T = JSONValue>(config: CuimpRequestConfig): Promise<CuimpResponse<T>>;
    requestStream(config: CuimpRequestConfig, handlers?: CuimpStreamHandlers): Promise<CuimpStreamResponse>;
    get<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    delete<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    head<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    options<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    post<T = JSONValue>(url: string, data?: CuimpRequestConfig['data'], config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    put<T = JSONValue>(url: string, data?: CuimpRequestConfig['data'], config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    patch<T = JSONValue>(url: string, data?: CuimpRequestConfig['data'], config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
}

declare class Cuimp {
    private descriptor;
    private path;
    private binaryInfo?;
    private logger;
    private autoDownload;
    constructor(options?: CuimpOptions);
    /**
     * Verifies the binary is present and executable
     * Returns the binary path if found or downloads it
     */
    verifyBinary(): Promise<string>;
    /**
     * Checks if a binary file exists and is executable
     */
    private isBinaryExecutable;
    /**
     * Builds a command preview for the given URL and method
     */
    buildCommandPreview(url: string, method?: string): Promise<string>;
    /**
     * Gets the current binary path
     */
    getBinaryPath(): string;
    /**
     * Gets the current descriptor
     */
    getDescriptor(): CuimpDescriptor;
    /**
     * Gets binary information if available
     */
    getBinaryInfo(): BinaryInfo | undefined;
    /**
     * Updates the descriptor
     */
    setDescriptor(descriptor: CuimpDescriptor): void;
    /**
     * Sets a custom binary path
     */
    setBinaryPath(path: string): void;
    /** Convenience to ensure binary and return verified path */
    ensurePath(): Promise<string>;
    /**
     * Downloads the binary without verifying it
     * Useful for pre-downloading or explicit download control
     */
    download(): Promise<BinaryInfo>;
}

/**
 * Manages cookie storage for curl-impersonate requests.
 * Uses curl's native cookie file format (Netscape format).
 */
declare class CookieJar {
    private cookieFilePath;
    private isTemporary;
    private initialized;
    constructor(option: CookieJarOption);
    /**
     * Initialize the cookie file if it doesn't exist
     */
    private ensureFileExists;
    /**
     * Get the path to the cookie file
     */
    getFilePath(): string;
    /**
     * Get curl arguments for cookie handling
     * -b: Read cookies from file
     * -c: Write cookies to file
     */
    getCurlArgs(): string[];
    /**
     * Clear all cookies by emptying the cookie file
     */
    clear(): void;
    private isLineComment;
    /**
     * Get all cookies as raw text (Netscape format)
     */
    getCookiesRaw(): string;
    private domainCleaner;
    /**
     * Parse cookies from the file into an array of cookie objects
     */
    getCookies(): Array<{
        domain: string;
        includeSubdomains: boolean;
        path: string;
        secure: boolean;
        expires: number;
        name: string;
        value: string;
    }>;
    /**
     * Get cookies for a specific domain
     */
    getCookiesForDomain(domain: string): Array<{
        name: string;
        value: string;
    }>;
    /**
     * Set a cookie manually (writes to the cookie file)
     */
    setCookie(options: {
        domain: string;
        path?: string;
        secure?: boolean;
        expires?: number | Date;
        name: string;
        value: string;
        includeSubdomains?: boolean;
    }): void;
    /**
     * Delete cookies by name (optionally filtered by domain)
     */
    deleteCookie(name: string, domain?: string): void;
    /**
     * Clean up the cookie file (for temporary files)
     */
    destroy(): void;
    /**
     * Check if the cookie jar is using a temporary file
     */
    isTemp(): boolean;
}

declare class CuimpHttp implements CuimpInstance {
    private core;
    private defaults;
    private cookieJar;
    constructor(core: Cuimp, defaults?: Partial<CuimpRequestConfig>, cookieJarOption?: CookieJarOption);
    /**
     * Get the cookie jar instance (if enabled)
     */
    getCookieJar(): CookieJar | null;
    /**
     * Clear all cookies in the jar
     */
    clearCookies(): void;
    /**
     * Destroy the client and clean up resources (including temporary cookie files)
     */
    destroy(): void;
    private buildRequestParts;
    request<T = JSONValue>(config: CuimpRequestConfig): Promise<CuimpResponse<T>>;
    requestStream(config: CuimpRequestConfig, handlers?: CuimpStreamHandlers): Promise<CuimpStreamResponse>;
    get<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    delete<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    head<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    options<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    post<T = JSONValue>(url: string, data?: CuimpRequestConfig['data'], config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    put<T = JSONValue>(url: string, data?: CuimpRequestConfig['data'], config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
    patch<T = JSONValue>(url: string, data?: CuimpRequestConfig['data'], config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
}

/**
 * Curl exit codes from libcurl
 * See: https://curl.se/libcurl/c/libcurl-errors.html
 */
declare enum CurlExitCode {
    OK = 0,
    UNSUPPORTED_PROTOCOL = 1,
    FAILED_INIT = 2,
    URL_MALFORMAT = 3,
    NOT_BUILT_IN = 4,
    COULDNT_RESOLVE_PROXY = 5,
    COULDNT_RESOLVE_HOST = 6,
    COULDNT_CONNECT = 7,
    WEIRD_SERVER_REPLY = 8,
    REMOTE_ACCESS_DENIED = 9,
    FTP_ACCEPT_FAILED = 10,
    FTP_WEIRD_PASS_REPLY = 11,
    FTP_ACCEPT_TIMEOUT = 12,
    FTP_WEIRD_PASV_REPLY = 13,
    FTP_WEIRD_227_FORMAT = 14,
    FTP_CANT_GET_HOST = 15,
    HTTP2 = 16,
    FTP_COULDNT_SET_TYPE = 17,
    PARTIAL_FILE = 18,
    FTP_COULDNT_RETR_FILE = 19,
    QUOTE_ERROR = 21,
    HTTP_RETURNED_ERROR = 22,
    WRITE_ERROR = 23,
    UPLOAD_FAILED = 25,
    READ_ERROR = 26,
    OUT_OF_MEMORY = 27,
    OPERATION_TIMEDOUT = 28,
    FTP_PORT_FAILED = 30,
    FTP_COULDNT_USE_REST = 31,
    RANGE_ERROR = 33,
    SSL_CONNECT_ERROR = 35,
    BAD_DOWNLOAD_RESUME = 36,
    FILE_COULDNT_READ_FILE = 37,
    LDAP_CANNOT_BIND = 38,
    LDAP_SEARCH_FAILED = 39,
    ABORTED_BY_CALLBACK = 42,
    BAD_FUNCTION_ARGUMENT = 43,
    INTERFACE_FAILED = 45,
    TOO_MANY_REDIRECTS = 47,
    UNKNOWN_OPTION = 48,
    SETOPT_OPTION_SYNTAX = 49,
    GOT_NOTHING = 52,
    SSL_ENGINE_NOTFOUND = 53,
    SSL_ENGINE_SETFAILED = 54,
    SEND_ERROR = 55,
    RECV_ERROR = 56,
    SSL_CERTPROBLEM = 58,
    SSL_CIPHER = 59,
    PEER_FAILED_VERIFICATION = 60,
    BAD_CONTENT_ENCODING = 61,
    FILESIZE_EXCEEDED = 63,
    USE_SSL_FAILED = 64,
    SEND_FAIL_REWIND = 65,
    SSL_ENGINE_INITFAILED = 66,
    LOGIN_DENIED = 67,
    TFTP_NOTFOUND = 68,
    TFTP_PERM = 69,
    REMOTE_DISK_FULL = 70,
    TFTP_ILLEGAL = 71,
    TFTP_UNKNOWNID = 72,
    REMOTE_FILE_EXISTS = 73,
    TFTP_NOSUCHUSER = 74,
    SSL_CACERT_BADFILE = 77,
    REMOTE_FILE_NOT_FOUND = 78,
    SSH = 79,
    SSL_SHUTDOWN_FAILED = 80,
    AGAIN = 81,
    SSL_CRL_BADFILE = 82,
    SSL_ISSUER_ERROR = 83,
    FTP_PRET_FAILED = 84,
    RTSP_CSEQ_ERROR = 85,
    RTSP_SESSION_ERROR = 86,
    FTP_BAD_FILE_LIST = 87,
    CHUNK_FAILED = 88,
    NO_CONNECTION_AVAILABLE = 89,
    SSL_PINNEDPUBKEYNOTMATCH = 90,
    SSL_INVALIDCERTSTATUS = 91,
    HTTP2_STREAM = 92,
    RECURSIVE_API_CALL = 93,
    AUTH_ERROR = 94,
    HTTP3 = 95,
    QUIC_CONNECT_ERROR = 96,
    PROXY = 97,
    SSL_CLIENTCERT = 98,
    UNRECOVERABLE_POLL = 99,
    TOO_LARGE = 100,
    ECH_REQUIRED = 101
}
/**
 * Custom error class for curl execution errors
 */
declare class CurlError extends Error {
    readonly code: CurlExitCode;
    readonly stderr?: string | undefined;
    constructor(code: CurlExitCode, stderr?: string | undefined, message?: string);
}

interface RunResult {
    exitCode: CurlExitCode | null;
    stdout: Buffer;
    stderr: Buffer;
}
interface RunStreamResult {
    exitCode: CurlExitCode | null;
    stderr: Buffer;
}

declare function runBinary(binPath: string, args: string[], opts?: {
    timeout?: number;
    signal?: AbortSignal;
    stdin?: string | Buffer;
}): Promise<RunResult>;
declare function runBinaryStream(binPath: string, args: string[], opts?: {
    timeout?: number;
    signal?: AbortSignal;
    stdin?: string | Buffer;
    onStdout?: (chunk: Buffer) => void | Promise<void>;
    onStderr?: (chunk: Buffer) => void | Promise<void>;
}): Promise<RunStreamResult>;

declare function createCuimpHttp(options?: CuimpOptions): CuimpHttp;
declare function request<T = JSONValue>(config: CuimpRequestConfig): Promise<CuimpResponse<T>>;
declare function requestStream(config: CuimpRequestConfig, handlers?: CuimpStreamHandlers): Promise<CuimpStreamResponse>;
declare function get<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
declare function post<T = JSONValue>(url: string, data?: CuimpRequestConfig['data'], config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
declare function put<T = JSONValue>(url: string, data?: CuimpRequestConfig['data'], config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
declare function patch<T = JSONValue>(url: string, data?: CuimpRequestConfig['data'], config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
declare function del<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
declare function head<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
declare function options<T = JSONValue>(url: string, config?: Omit<CuimpRequestConfig, 'url' | 'method' | 'data'>): Promise<CuimpResponse<T>>;
declare function downloadBinary(options?: CuimpOptions): Promise<BinaryInfo>;
declare function __smoke(): Promise<void>;

export { type BinaryInfo, CookieJar, type CookieJarOption, Cuimp, type CuimpDescriptor, CuimpHttp, type CuimpInstance, type CuimpOptions, type CuimpRequestConfig, type CuimpResponse, type CuimpStreamHandlers, type CuimpStreamHeaders, type CuimpStreamResponse, CurlError, CurlExitCode, type Method, type RunResult, type RunStreamResult, __smoke, createCuimpHttp, del, downloadBinary, get, head, options, patch, post, put, request, requestStream, runBinary, runBinaryStream };
